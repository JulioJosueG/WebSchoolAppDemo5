@model IEnumerable<WebSchoolAppUI.Models.Departamento>

@{
    ViewData["Title"] = "Index";
}

<h1>Listado de Departamentos</h1>

<p>
    <a asp-action="Create">Nuevo departamento</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
    Distrito
</th>
            
            <th>Acción</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdDistritoNavigation.Codigo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDepartamento">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDepartamento">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDepartamento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
